{
  "name": "zelda-overlay",
  "private": true,
  "type": "module",
  "description": "Zelda: A Link to the Past overlay extension for the Pi coding agent.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "smoke": "node --test tests/*.test.mjs",
    "setup:core": "bash ./setup.sh",
    "refresh:core": "bash ./setup.sh --force"
  },
  "engines": {
    "node": ">=20"
  }
}
